#include <bits/stdc++.h>
using namespace std;

int main()
{
    int test = 0;
    cin >> test;

    while (test--)
    {
        int n, k;
        cin >> n >> k;

        vector<int> v(n);

       
    }
    return 0;
}